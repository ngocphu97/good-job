import { ThumnailDirective } from './thumnail.directive';

describe('ThumnailDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumnailDirective();
    expect(directive).toBeTruthy();
  });
});
